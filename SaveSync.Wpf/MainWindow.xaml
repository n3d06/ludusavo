<ui:FluentWindow x:Class="SaveSync.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:SaveSync.Desktop.ViewModels"
        xmlns:pages="clr-namespace:SaveSync.Desktop.Views.Pages"
        Title="{Binding Title}"
        Icon="icon.ico"
        Height="720" Width="1080"
        MinHeight="550" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        WindowBackdropType="Mica"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Title}" />

        <ui:NavigationView Grid.Row="1"
                           x:Name="RootNavigation"
                           PaneDisplayMode="Left"
                           IsBackButtonVisible="Collapsed"
                           ScrollViewer.VerticalScrollBarVisibility="Disabled"
                           ScrollViewer.CanContentScroll="False">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Game trên máy" Icon="{ui:SymbolIcon Library24}" TargetPageType="{x:Type pages:GamesPage}" />
                <ui:NavigationViewItem Content="Sao lưu Cloud" Icon="{ui:SymbolIcon Cloud24}" TargetPageType="{x:Type pages:CloudPage}" />
            </ui:NavigationView.MenuItems>
            <ui:NavigationView.FooterMenuItems>
                <ui:NavigationViewItem Content="Cài đặt" Icon="{ui:SymbolIcon Settings24}" TargetPageType="{x:Type pages:SettingsPage}" />
            </ui:NavigationView.FooterMenuItems>
        </ui:NavigationView>

        <!-- GitHub Connection Status Badge -->
        <Border Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,5"
                Margin="0,0,20,20"
                Panel.ZIndex="1">
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="8" Height="8"
                         Fill="{Binding IsGitHubConnected, Converter={StaticResource BoolToBrushConverter}}"
                         Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding GitHubStatusText}"
                           FontSize="12"
                           FontWeight="Medium"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
