<Page x:Class="SaveSync.Desktop.Views.Pages.GamesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:SaveSync.Desktop.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance vm:GamesViewModel}"
      Title="GamesPage"
      Background="Transparent">

    <Grid Margin="20,0,20,16">
        <Grid.RowDefinitions>
            <!-- Toolbar: Search, Refresh, SyncAll -->
            <RowDefinition Height="Auto"/>
            <!-- Scanning Status Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Games List -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box with Native Vector Path Icon -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <TextBox x:Name="SearchBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="34"
                         VerticalContentAlignment="Center"
                         Padding="34,0,10,0"/>

                <!-- Native Vector Search Icon (Lucide-style Path) with dynamic focus stroke -->
                <Path Width="14" Height="14"
                      Stretch="Uniform"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Center"
                      Margin="12,0,0,0"
                      IsHitTestVisible="False"
                      StrokeThickness="1.8"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      Data="M 21 21 L 16.65 16.65 M 19 11 A 8 8 0 1 1 3 11 A 8 8 0 1 1 19 11 Z">
                    <Path.Style>
                        <Style TargetType="Path">
                            <Setter Property="Stroke" Value="{DynamicResource TextFillColorTertiaryBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsKeyboardFocusWithin, ElementName=SearchBox}" Value="True">
                                    <Setter Property="Stroke" Value="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Path.Style>
                </Path>

                <!-- Placeholder text -->
                <TextBlock Text="Tìm kiếm game..."
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="36,0,0,0"
                           IsHitTestVisible="False"
                           FontSize="13"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <Button Grid.Column="1"
                    Content="Quét lại"
                    Command="{Binding RefreshScanCommand}"
                    IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"
                    Height="34"
                    Padding="16,0"
                    Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="Đồng bộ tất cả"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SyncAllCommand}"
                    IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBooleanConverter}, FallbackValue=True}"
                    Height="34"
                    Padding="16,0"/>
        </Grid>

        <!-- Scanning Status Bar -->
        <Border Grid.Row="1"
                Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16,12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Text="{Binding ScanStatusText}"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           Margin="0,0,0,8"/>
                <ProgressBar Grid.Row="1"
                             Value="{Binding ScanProgress}"
                             Height="6"
                             IsIndeterminate="{Binding ScanProgress, Converter={StaticResource ZeroToIndeterminateConverter}, FallbackValue=False}"/>
            </Grid>
        </Border>

        <!-- Games List (Subtle floating cards on single background) -->
        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Games}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,10"
                                Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Game Image Banner (Steam Header) -->
                                <Border Grid.Column="0"
                                        Width="140"
                                        Height="65"
                                        CornerRadius="6"
                                        ClipToBounds="True"
                                        Background="#E2E8F0"
                                        Margin="0,0,16,0">
                                    <Grid>
                                        <Image Source="{Binding SteamBannerUrl}"
                                               Stretch="UniformToFill"/>
                                        <TextBlock Text="{Binding Name}"
                                                   Visibility="{Binding SteamBannerUrl, Converter={StaticResource NullToVisibilityConverter}}"
                                                   Foreground="#64748B"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Center"
                                                   Margin="4"/>
                                    </Grid>
                                </Border>

                                <!-- Game Details -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="15"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                   VerticalAlignment="Center"/>

                                        <!-- Status Badge -->
                                        <Border Background="{Binding StatusBadgeColor}"
                                                CornerRadius="4"
                                                Padding="8,2"
                                                Margin="10,0,0,0"
                                                VerticalAlignment="Center">
                                            <TextBlock Text="{Binding StatusBadgeText}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="White"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding FileCount, StringFormat='{}{0} files'}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   Margin="0,0,16,0"/>

                                        <TextBlock Text="{Binding TotalSizeFormatted}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   Margin="0,0,16,0"/>

                                        <TextBlock Text="{Binding LastModifiedFormatted, StringFormat='Lần sửa cuối: {0}'}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                    </StackPanel>

                                    <!-- Status message when working -->
                                    <TextBlock Text="{Binding StatusMessage}"
                                               FontSize="11"
                                               Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                                               Margin="0,4,0,0"
                                               Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                                </StackPanel>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="2"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">

                                    <Button Content="Mở thư mục"
                                            Command="{Binding DataContext.OpenSaveFolderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Margin="0,0,8,0"
                                            Padding="14,0"
                                            Height="34"/>

                                    <Button Content="Sao lưu lên GitHub"
                                            Style="{StaticResource PrimaryButton}"
                                            Command="{Binding DataContext.SyncGameCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
                                            Padding="16,0"
                                            Height="34"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
